---
// Scene 5: Comparison with Baselines
// Stub for L21 when baseline comparisons land
---

<section id="comparison" class="section-divider">
  <div class="prose-section">
    <div class="mb-4">
      <span class="scene-label">Scene 5</span>
    </div>
    <h2>Steering for alignment & against jailbreaks: how it compares</h2>

    <p class="text-lg text-ink-soft">
      A natural question: does this approach beat simpler baselines? How does it scale to real
      alignment objectives?
    </p>

    <div class="card my-8" style="border-color: var(--amber); border-width: 2px; background-color: rgba(245, 158, 11, 0.05);">
      <p class="font-semibold mb-2 text-amber">🔬 Results landing: L21 gates pending</p>
      <p class="text-sm text-ink-soft">
        This section is a clearly-labeled placeholder. L21 baseline comparisons
        (logit-bias, prompt engineering, LoRA fine-tune) are pre-registered and will land here
        with real data when the runs complete. We ship honest placeholders, not filled fakes.
      </p>
    </div>

    <div class="grid md:grid-cols-2 gap-4">
      <div class="card">
        <h3 class="font-semibold text-sm mb-2 text-cyan">Baseline 1: Logit bias</h3>
        <p class="text-sm text-ink-soft">Bias the output softmax toward registered tokens.</p>
        <div class="mt-4 pt-4" style="border-top: 1px solid var(--hairline);">
          <div class="text-xs text-ink-faint">📋 gate_L21_logitbias — pending</div>
        </div>
      </div>

      <div class="card">
        <h3 class="font-semibold text-sm mb-2 text-cyan">Baseline 2: Prompt steering</h3>
        <p class="text-sm text-ink-soft">Prepend role/instruction prompts to the input.</p>
        <div class="mt-4 pt-4" style="border-top: 1px solid var(--hairline);">
          <div class="text-xs text-ink-faint">📋 gate_L21_prompt — pending</div>
        </div>
      </div>

      <div class="card">
        <h3 class="font-semibold text-sm mb-2 text-cyan">Baseline 3: LoRA fine-tune</h3>
        <p class="text-sm text-ink-soft">Low-rank adapter trained on steer+concept pairs.</p>
        <div class="mt-4 pt-4" style="border-top: 1px solid var(--hairline);">
          <div class="text-xs text-ink-faint">📋 gate_L21_lora — pending</div>
        </div>
      </div>

      <div class="card" style="background: rgba(168, 85, 247, 0.08); border-color: var(--violet); border-width: 2px;">
        <h3 class="font-semibold text-sm mb-2 text-violet">Our method: Band-targeted steering</h3>
        <p class="text-sm text-ink-soft">
          Recognition-gated Jacobian writes at sphuraṭṭā moments.
        </p>
        <div class="mt-4 pt-4" style="border-top: 1px solid var(--violet-dim);">
          <div class="text-xs font-semibold" style="color: var(--accept);">✓ Freeze-time friendly</div>
          <p class="text-xs text-ink-soft">No training needed; works on frozen models</p>
        </div>
      </div>
    </div>

    <div class="mt-8 p-4 panel" style="background-color: var(--bg-elevated);">
      <strong style="color: var(--violet);">Next steps:</strong>
      <p class="text-sm text-ink-soft mt-2">
        Parametric alignment steering (truthfulness, refusal),
        cross-episode continuity (does CittaStore state persist?),
        W-space generalization (does this work beyond text?).
      </p>
    </div>
  </div>
</section>
